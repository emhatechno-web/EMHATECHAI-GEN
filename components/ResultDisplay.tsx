import React from 'react';

// This component is currently not in use.
// It has been given a basic structure to prevent build errors from an empty file.
export const ResultDisplay: React.FC = () => {
  return null;
};
